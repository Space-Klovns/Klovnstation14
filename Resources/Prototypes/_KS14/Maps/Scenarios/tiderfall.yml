- type: gameMap
  id: Tiderfall
  mapName: 'Tiderfall'
  mapPath: /Maps/_KS14/Scenarios/Tiderfall/tiderfall.yml
  minPlayers: 0
  maxPlayers: 30
  stations:
    TiderfallCanyon:
      stationProto: ScenarioTiderfallStationProto
      components:
        - type: StationNameSetup
          mapNameTemplate: 'Tiderfall (you should not see this)'
        - type: StationJobs
          availableJobs: #tiders, testing
            Passenger: [ -1, -1 ] #infinite, not counted
